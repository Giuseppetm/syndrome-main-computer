import { Link } from "react-router-dom";

const Supers = () => {
    return (
        <section id="supers">

            <Link to={`/supers/0`}>
                Supers list
            </Link>
            
        </section>
    );
};

export default Supers;